import { Model } from '@cwqt/refract'

export const User = Model('User')
export const Profile = Model('Profile')
export const Post = Model('Post')
export const Category = Model('Category')

export default [User, Profile, Post, Category]
